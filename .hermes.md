# HexMap 项目约定（Hermes）

> 本文件在 Assets 及子目录下的 Hermes 会话中自动加载。
> 通用规则见 AGENT.md；本文件只放 Hermes 专属约定。

## 项目事实
- 技术栈：Unity 2022.3 + 自定义 SRP（CustomRP 只渲染 SRPDefaultUnlit pass）
- **新材质必须用 Unlit shader**，否则渲染不可见
- Catlike HexMap 教程实现，当前进度见 `docs/README.md`
- 关键脚本：HexGrid / HexCell / HexMesh / HexMetrics / HexCoordinates / HexDirection / HexMapEditor

## 关键约定：代码同步 CodeTxts
- 开发环境对 .cs 加密、.txt 不加密；`Assets/CodeTxts/` 是 .cs 的未加密镜像，供 git 提交
- 改完 .cs 必须运行菜单 `Tools/代码同步/导出 .cs 为 .txt (CodeTxts)` 刷新镜像
- 只改 CodeTxts 不改 .cs 等于没改

## 文档约定
- 每功能一篇：`docs/<feature>.md`，结构：目标 / 当前状态 / 设计要点 / 接口依赖 / 验证方式 / 变更记录
- 开工前先读 `docs/README.md` 和相关功能文档
- 功能完成后同步更新对应文档的"当前状态"与"变更记录"

## 会话习惯
- 一次会话聚焦一个功能主题
- 涉及角色控制的改动先读 `docs/charactercontrol.md`
- 大改造先给方案分析，用户确认后再动手（用户偏好）
